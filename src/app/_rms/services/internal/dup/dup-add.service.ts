import {Injectable} from '@angular/core';
import {DupInterface} from '../../../interfaces/dup/dup.interface';


@Injectable({providedIn: 'root'})
export class DupAddService {
    constructor() {
    }

    addDup(dup: DupInterface) {

    }
}
